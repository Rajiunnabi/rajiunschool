@model List<subjectrequestextend>

<style>
    .container {
        margin-top: 50px;
        position: relative;
    }

    .top-right-btn {
        position: absolute;
        top: 0;
        right: 0;
        margin: 10px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th, td {
        padding: 10px;
        border: 1px solid #ddd;
        text-align: center;
    }

    th {
        background-color: #007bff;
        color: white;
    }

    .btn-danger, .btn-primary {
        padding: 5px 10px;
        text-decoration: none;
        color: white;
        border-radius: 5px;
    }

    .btn-danger {
        background-color: red;
    }

    .btn-primary {
        background-color: #007bff;
    }

    .search-container {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
    }

    .search-input {
        width: 300px;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 5px;
    }

    .search-button {
        padding: 8px 15px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

        .search-button:hover {
            background-color: #0056b3;
        }

    .reset-button {
        padding: 8px 15px;
        background-color: gray;
        color: white;
        border: none;
        border-radius: 5px;
        text-decoration: none;
        margin-left: 10px;
    }

        .reset-button:hover {
            background-color: darkgray;
        }
</style>

<div class="container">
    <h2 class="text-center mb-4">Subject Requests</h2>

    <!-- Search Bar -->
    <div class="search-container">
        <form method="get" action="@Url.Action("SubjectRequest", "CrudTable")">
            <input type="text" name="searchQuery" class="search-input" placeholder="Search by Subject ID or Name" />
            <button type="submit" class="search-button">Search</button>
            <a href="@Url.Action("SubjectRequest", "CrudTable")" class="reset-button">Reset</a>
        </form>
    </div>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Subject ID</th>
                <th>Subject Name</th>
                <th>Department</th>
                <th>Semester</th>
                <th>Instructor</th>
                <th>View Profile</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var subjectrequest in Model)
            {
                <tr>
                    <td>@subjectrequest.subject.id</td>
                    <td>@subjectrequest.subject.subjectname</td>
                    <td>@subjectrequest.subject.dept</td>
                    <td>@subjectrequest.subject.semester</td>
                    <td>@subjectrequest.teacherid</td>
                    <td>
                        <a href="@Url.Action("Details", "User", new { id = subjectrequest.teacherid })" class="btn btn-primary">View Profile</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
